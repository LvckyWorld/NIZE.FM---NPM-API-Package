{
  "name": "@nize.fm/api",
  "version": "1.0.1",
  "description": "The official NPM API Package of the NIZE.FM API",
  "main": "lib/NizeAPI.js",
  "types": "types/NizeAPI.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npx tsc",
    "ci-unpublish": "npm unpublish --force",
    "ci-publish": "npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "https://git.lvckyworld.dev/nize.fm/npm-api-package.git"
  },
  "author": "LvckyWorld",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
